# 前端环境变量（Vite 注入，需 VITE_ 前缀）
# Worker 的基础 URL。开发时本地 wrangler dev 默认 http://localhost:8787。
# 生产环境：你的 Cloudflare Worker 域名，如 https://gaubee-auth.workers.dev 或 https://auth.gaubee.com
VITE_AUTH_BASE=http://localhost:8787

# Worker 的环境变量（在 worker/.dev.vars 或 wrangler secret 配置，不提交）：
# GITHUB_CLIENT_ID     - GitHub OAuth App 的 Client ID
# GITHUB_CLIENT_SECRET - GitHub OAuth App 的 Client Secret
# APP_ORIGIN           - 静态站点 origin（CORS 与重定向白名单），如 http://localhost:5173 或 https://gaubee.com
